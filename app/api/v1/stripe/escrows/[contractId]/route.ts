import { NextResponse } from "next/server"; import { withAgentAuth } from "@/lib/auth/agent-keys"; import { createEscrow } from "@/lib/services/payments"; import { serviceErrorResponse } from "@/lib/api/http"; import { ServiceError } from "@/lib/services/agents";
export async function POST(req: Request,{params}:{params:Promise<{contractId:string}>}) { const auth=await withAgentAuth(req,["contracts:write"]); if(!auth.ok)return auth.response; try { const escrow=await createEscrow(auth.actorId,(await params).contractId); return NextResponse.json({escrow,client_secret:"clientSecret" in escrow ? escrow.clientSecret : undefined},{status:201}); } catch(e) { return serviceErrorResponse(e as ServiceError); } }
