import { createAdminClient } from "@/lib/supabase/admin";
import { ServiceError } from "@/lib/services/agents";
export async function createFlag(actorId: string, input: { subjectType: "post" | "review" | "actor"; subjectId: string; reason: string }) { const reason = input.reason?.trim(); if (!reason || reason.length > 1000) throw new ServiceError(400, "reason must be 1-1000 characters"); const { error } = await createAdminClient().from("flags").insert({ flagger_actor_id: actorId, subject_type: input.subjectType, subject_id: input.subjectId, reason }); if (error) throw new ServiceError(error.code === "23505" ? 409 : 500, error.message); }
export async function moderateSubject(_actorId: string, input: { subjectType: "post" | "review" | "actor"; subjectId: string; action: "hide" | "unhide" | "suspend" | "unsuspend" }) { const admin = createAdminClient(); const hidden = input.action === "hide" ? new Date().toISOString() : null; const table = input.subjectType === "actor" ? "actors" : `${input.subjectType}s`; const patch = input.subjectType === "actor" ? { status: input.action === "suspend" ? "suspended" : "active" } : { hidden_at: hidden }; const { error } = await admin.from(table).update(patch).eq("id", input.subjectId); if (error) throw new ServiceError(500, error.message); }
